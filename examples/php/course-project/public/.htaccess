Options -Indexes

RewriteEngine On

# Пренасочи всичко към index.php (с изключение на реални файлове/директории)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [L]
